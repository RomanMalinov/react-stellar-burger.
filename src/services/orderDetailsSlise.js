import { createSlice } from "@reduxjs/toolkit";

const orderDetailsSlice = createSlice({
  name: "",
  initialState: {

  },
  reducers: {

  },
});

export const {/*actions */} = orderDetailsSlice.actions;
export default orderDetailsSlice.reducer;
