import { createSlice } from "@reduxjs/toolkit";

const ingredientSlice = createSlice({
  name: "",
  initialState: {

  },
  reducers: {

  },
});

export const {/*actions */} = ingredientSlice.actions;
export default ingredientSlice.reducer;
